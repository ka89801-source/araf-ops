# حالة تنفيذ المنصة المستقلة

## النتيجة المطلوبة

منصة عمليات فعلية بكامل الطلبات والأعمال، على قاعدة بيانات مستقلة مستنسخة من الأصل. أي تعديل أو حذف في الجديدة يجب ألا يؤثر على الأصل. الفاحص يستخدم مساعد أعراف القائم بدل اعتماد بيئة Claude. هذه المرحلة ليست إعلانًا لاكتمال الربط.

## ما نُفذ في هذه المرحلة

- نُقلت الواجهة المرفقة كاملة مع مصادرها، دون تغيير التصميم أو التفاعلات.
- أصبح البناء قابلًا للتشغيل من أي مسار، مع فحص تطابق ملف العرض والمصدر.
- جميع تغييرات المستودع محصورة في برنش جديد؛ لم تُنفذ هجرة أو كتابة إلى قاعدة الإنتاج.

## ما تبين من المستودعات الحالية

- `araf-ops` يستخدم Supabase، إضافة إلى واجهات الإدارة في `ARAF-COMPANY` على `araf.company/api`.
- من الجداول المستخدمة في الكود: `service_requests`، `employees`، `request_activity_log`، `request_delete_requests`، `support_tickets`، `ops_reset_requests`، `business_entities`، `business_requests`، `business_usage`، `business_request_history`، `business_admins`. هذه ليست قائمة كاملة بقاعدة البيانات؛ يلزم فحص مخططها الفعلي.
- المساعد موجود في `ka89801-source/araf-site-main` في `api/free-ask.js`، عند الالتزام `e7701c15319ade33d1286f102a33287580a73b5d`.
- واجهته تقبل `POST` مع `{ query }` حتى 1000 حرف، وتعيد `content` بصيغة HTML و`sources` و`confidenceLevel`. تستخدم مفاتيح خادمية `OPENAI_API_KEY` و`SERPER_API_KEY`.
- الفاحص المرفق يتوقع تقرير JSON من `window.claude.use('sample')`؛ لذلك استبدال عنوان اتصال فقط لا يكفي. يلزم محوّل خلفي مصادق عليه يعيد بيانات الفاحص بالصيغة المطلوبة ويحافظ على مصادر المساعد، دون إظهار مفاتيح سرية في الواجهة.
- لم تُستدعَ واجهة المساعد الفعلية ولم تُرسل إليها بيانات طلبات؛ فحصها اقتصر على الكود.

## المتبقي والاعتماديات

1. ربط Supabase بالحساب صاحب قاعدة أعراف. اتصال GitHub وحده لا يتيح استنساخ بيانات PostgreSQL أو إدارة مشاريع Supabase.
2. جرد الجداول والمخططات والعلاقات والفهارس والسياسات والدوال والمشغلات والمستخدمين والتخزين. لا يُفترض أن نسخ ملفات SQL الجزئية في GitHub يعادل نسخة كاملة.
3. إنشاء وجهة منفصلة، والتحقق من اختلاف هوية المشروع والاتصال قبل الاستعادة. تُقرأ قاعدة الأصل فقط. تُعطّل في الوجهة أي جدولة أو webhook أو تكامل قد يُرسل إشعارات إلى عملاء الإنتاج.
4. نسخ البنية والبيانات والتحقق من الأعداد والعلاقات. حسابات المصادقة وملفات التخزين والدوال والأسرار تحتاج تحققًا منفصلًا؛ ليست كلها بالضرورة ضمن نسخة SQL.
5. ربط واجهة النسخة الجديدة بالوجهة المستقلة مع مصادقة وصلاحيات وحفظ فعلي لجميع الإجراءات. إزالة بيانات العرض من وضع التشغيل الفعلي، ومنع نجاح وهمي أو الرجوع إلى الإنتاج عند فشل الاتصال.
6. تنفيذ محوّل المساعد واختباره. لا تُرفع مفاتيح أو نسخ قواعد بيانات أو بيانات عملاء إلى هذا المستودع العام.
7. إذا أريد استمرار وصول الطلبات الجديدة بعد لحظة الاستنساخ، تُحدد وتنفذ مزامنة أحادية الاتجاه من الأصل إلى النسخة، بصلاحية قراءة على المصدر، مع سياسة تمنع محو تعديلات النسخة. النسخ لمرة واحدة وحده لا يوفر ذلك.
8. اختبار الإضافة والإسناد والتعليقات والحالات والدفع والحصص والأرشفة والتفعيل والصلاحيات واستمرار الحفظ، وإثبات عدم تغير بيانات الأصل نتيجة اختبارات النسخة.

## التحقق المنجز

- تطابق `index.html` بايتًا ببايت مع الملف المرفق.
- SHA-256 للعرض: `b1df5c378304837d86798dfbdb7ffbdfc6e1e2f8a3f0144ef047026bc043fd1d`.
- مطابقة ملفات المصدر للمرفق، باستثناء تعديل أداة البناء الموثق.
- نجاح تحليل JavaScript للملفات الثمانية بواسطة `node --check` ونجاح `build.py --check`.
- تعذر إجراء اختبار متصفح كامل لأن تنزيل Chromium لم ينجح في بيئة التنفيذ. لا توجد نتيجة اختبار اتصال فعلي أو استنساخ قاعدة بيانات حتى هذه المرحلة.
